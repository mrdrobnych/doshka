<% if !user_signed_in? %>
	<%= link_to "Login", user_session_path %>
<% else %>
	<%= link_to "My Profile", profile_path(current_user.id) %> | 
	<%= link_to "Logout", destroy_user_session_path, method: :delete %><br>
	<%= link_to "Add product", posts_path %>
<% end %>


